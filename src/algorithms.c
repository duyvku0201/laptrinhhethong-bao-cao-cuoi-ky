#include "algorithms.h"
#include <stdio.h>

void fcfs(Process p[], int n) { (void)p; (void)n; }
void sjf(Process p[], int n) { (void)p; (void)n; }
void srtf(Process p[], int n) { (void)p; (void)n; }
void priority_non_preemptive(Process p[], int n) { (void)p; (void)n; }
// priority_preemptive implementation is in src/algorithms/priority_p.c
// round_robin implementation is in src/algorithms/round_robin.c
// Do NOT implement stubs here to avoid multiple definition error
// round_robin implementation is in src/algorithms/round_robin.c